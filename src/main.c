#include "globale.h"
#include<stdio.h>
#include"struct.h"
#include<stdlib.h>

int main(void)
{
    FGlobale();
    /*
    FILE *fe;
    VilleIUT *ville;
    ville=(VilleIUT*)malloc(sizeof(VilleIUT));
    fe=fopen("data/IUT.don","r");
    if(fe==NULL)
    {
        printf("Error Opening File !!\n");
        return -1;
    }
    fscanf(fe,"%s %d\n",ville->VilleDep,&ville->nbDept);
    printf("%s\n",ville->VilleDep);
    
    */
    return 0;
}